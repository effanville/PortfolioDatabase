<UserControl x:Class="Effanville.FPD.UI.Windows.Common.DataNamesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:cht="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"
             xmlns:converters="clr-namespace:Effanville.Common.UI.Wpf.Converters;assembly=Effanville.Common.UI.Wpf"
             xmlns:controls="clr-namespace:Effanville.Common.UI.Wpf.Controls;assembly=Effanville.Common.UI.Wpf"
             xmlns:common="clr-namespace:Effanville.FPD.Logic.ViewModels.Common;assembly=Effanville.FPD.Logic"
             d:DataContext="{d:DesignInstance Type=common:DataNamesViewModel}"
             mc:Ignorable="d">
    <Grid Height="Auto"
          Width="Auto"
          VerticalAlignment="Stretch"
          HorizontalAlignment="Stretch">
        <Grid.Resources>
            <converters:StringToUKDateConverter x:Key="StringToUKDate" />
            <BooleanToVisibilityConverter x:Key="BoolToVis" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal"
                    Height="35"
                    Grid.Row="0"
                    Grid.Column="0">
            <Button Content="Add"
                    Command="{Binding AddCommand}"
                    ToolTip="Add new item" />
            <Button Content="Edit"
                    Command="{Binding EditCommand}"
                    ToolTip="Edit selected item" />
            <Button Content="Download"
                    Command="{Binding DownloadCommand}"
                    ToolTip="Press here to download data for selected" />
            <Button Content="Delete"
                    Command="{Binding DeleteCommand}"
                    ToolTip="Delete the highlighted" />
        </StackPanel>
        <DataGrid Grid.Row="1"
                  Grid.Column="0"
                  Margin="5"
                  Width="Auto"
                  HorizontalAlignment="Left"
                  ToolTip="Lists all items currently in the database. Select one to edit data."
                  SelectionUnit="FullRow"
                  SelectionMode="Single"
                  IsReadOnly="False"
                  CanUserDeleteRows="True"
                  CanUserAddRows="False"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding Path=DataNames, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  DataContext="{Binding}"
                  SelectionChanged="DataGrid_OnSelectionChanged"
                  ScrollViewer.VerticalScrollBarVisibility="Visible"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  KeyDown="DataGrid_KeyDown"
                  PreviewKeyDown="DataGrid_KeyDown">
            <DataGrid.Resources>
                <controls:BindingProxy x:Key="proxy"
                                       Data="{Binding}" />
            </DataGrid.Resources>
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"
                                              MaxWidth="100" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition MaxHeight="20" />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0"
                                   Grid.Column="0"
                                   Grid.ColumnSpan="2"
                                   Text="Account Details:"
                                   Margin="2" />
                        <TextBlock Grid.Row="1"
                                   Grid.Column="0"
                                   Text="Broker:"
                                   Margin="2" />
                        <TextBlock Grid.Row="1"
                                 Visibility="{Binding Data.DisplayBroker, Converter={StaticResource BoolToVis}, Source={StaticResource proxy}}"
                                 Grid.Column="1"
                                 Text="{Binding ModelData.Broker, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="2"/>
                        <TextBlock Grid.Row="2"
                                   Grid.Column="0"
                                   Text="Company:"
                                   Margin="2" />
                        <TextBlock Grid.Row="2"
                                 Grid.Column="1"
                                 Visibility="{Binding Data.DisplayCompany, Converter={StaticResource BoolToVis}, Source={StaticResource proxy}}"
                                 Text="{Binding ModelData.Company, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="2" />
                        <TextBlock Grid.Row="3"
                                   Grid.Column="0"
                                   Text="Name:"
                                   Margin="2" />
                        <TextBlock Grid.Row="3"
                                 Grid.Column="1"
                                 Text="{Binding ModelData.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="2" />
                        <TextBlock Grid.Row="4"
                                   Grid.Column="0"
                                   Text="Url:"
                                   Margin="2" />
                        <TextBlock Grid.Row="4"
                                 Grid.Column="1"
                                 Text="{Binding ModelData.Url, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="2" />
                        <TextBlock Grid.Row="5"
                                   Grid.Column="0"
                                   Text="Currency:"
                                   Margin="2" />
                        <TextBlock Grid.Row="5"
                                 Grid.Column="1"
                                 Text="{Binding ModelData.Currency, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="2"/>
                        <TextBlock Grid.Row="6"
                                   Grid.Column="0"
                                   Text="Sectors:"
                                   Margin="2" />
                        <TextBlock Grid.Row="6"
                                 Grid.Column="1"
                                 Text="{Binding ModelData.Sectors, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="2" />
                        <TextBlock Grid.Row="7"
                                   Grid.Column="0"
                                   Text="Notes:"
                                   Margin="2" />
                        <TextBlock Grid.Row="7"
                                 Grid.Column="1"
                                 Text="{Binding ModelData.Notes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="2"/>
                    </Grid>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsUpdated}"
                                     Value="False">
                            <Setter Property="Background"
                                    Value="#FF5555" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.CellStyle>
                <Style TargetType="{x:Type DataGridCell}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsUpdated}"
                                     Value="False">
                            <Setter Property="Background"
                                    Value="#FF5555" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="RowEditEnding">
                    <i:InvokeCommandAction Command="{Binding CreateCommand}"
                                           CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type DataGrid}}, Mode=Default, Path=CurrentItem}" />
                </i:EventTrigger>
                <i:EventTrigger EventName="Delete">
                    <i:InvokeCommandAction Command="{Binding DeleteCommand}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <DataGrid.Columns>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel DataContext="{Binding}">
                                <Button Content="View Data"
                                        Style="{StaticResource DefaultButtonStyle}"
                                        DataContext="{Binding}"
                                        BorderThickness="0"
                                        Margin="0.5"
                                        Padding="3"
                                        Command="{Binding Path=DataContext.OpenTabCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Broker"
                                    MinWidth="70"
                                    Width="Auto"
                                    Visibility="{Binding Data.DisplayBroker, Converter={StaticResource BoolToVis}, Source={StaticResource proxy}}"
                                    Binding="{Binding Path=ModelData.Broker, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridTextColumn Header="Company"
                                    MinWidth="70"
                                    Width="Auto"
                                    Visibility="{Binding Data.DisplayCompany, Converter={StaticResource BoolToVis}, Source={StaticResource proxy}}"
                                    Binding="{Binding Path=ModelData.Company, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridTextColumn Header="Name"
                                    MinWidth="70"
                                    Width="Auto"
                                    Binding="{Binding Path=ModelData.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </DataGrid.Columns>
        </DataGrid>
        <GridSplitter Grid.Row="1"
                      Grid.Column="1"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Stretch"
                      ShowsPreview="True"
                      Width="5" />
        <Grid Grid.Row="1"
              Grid.Column="2"
              Visibility="{Binding Path=SelectedNameSet, Converter={StaticResource BoolToVis}}"
              Width="Auto"
              Height="Auto"
              VerticalAlignment="Stretch"
              HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <cht:Chart Title="Holding History"
                       Style="{StaticResource NoLegendChart}"
                       Height="Auto"
                       Width="Auto"
                       VerticalAlignment="Stretch"
                       Grid.Row="0"
                       Grid.RowSpan="2"
                       Grid.Column="0">
                <cht:Chart.Series>
                    <cht:LineSeries ItemsSource="{Binding Path=SelectedValueHistory}"
                                    DataPointStyle="{StaticResource InvisibleDataPointPrimary}"
                                    IndependentValueBinding="{Binding Path=Day}"
                                    DependentValueBinding="{Binding Path=Value}" />
                </cht:Chart.Series>
            </cht:Chart>
        </Grid>
    </Grid>
</UserControl>
