<StackPanel x:Class="FinanceWindows.BasicDataView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="450" 
      d:DesignWidth="800">
    <StackPanel Orientation="Horizontal"
                Margin="10 5 5 5">
        <StackPanel ToolTip="Lists all securities currently in the database. Select one to edit data.">
            <Label Content="Securities in Database"/>
            <ListView Name="SecBox"
                      ItemsSource="{Binding FundNames}"
                      MinHeight="300"
                      Margin="10,10,10,10"
                      MinWidth="210"
                      Width="210">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="100" 
                                        Header="Company" 
                                        DisplayMemberBinding="{Binding Company}"/>
                        <GridViewColumn Width="100" 
                                        Header="Name" 
                                        DisplayMemberBinding="{Binding Name}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>

        <StackPanel>
            <Label Content="Bank Accounts in Database"/>
            <ListView Name="BankAccounts"
                      ItemsSource="{Binding AccountNames}"
                      MinHeight="300"
                      Margin="10,10,10,10"
                      MinWidth="210"
                      Width="210">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="100" 
                                        Header="Company" 
                                        DisplayMemberBinding="{Binding Company}"/>
                        <GridViewColumn Width="100" 
                                        Header="Name" 
                                        DisplayMemberBinding="{Binding Name}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>

    </StackPanel>
</StackPanel>
